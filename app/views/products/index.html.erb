<% if !cart.empty? %>
    <ul>
        <%= cart.each do |item| %>
            <li><%= item %></li>
        <% end %>
    </ul>
<% end %>

<%= form_tag products_path do %>
    <label>Product:</label>
    <br>
    <%= text_field_tag :product %>
    <br>
    <%= submit_tag "add to cart"%>
<% end %>